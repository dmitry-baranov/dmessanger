package app.dmessanger.auth

/**
 * Authentication module placeholder.
 *
 * Planned responsibilities:
 * - login/password authentication for pre-created users;
 * - short-lived access tokens;
 * - refresh token rotation with server-side token hashes;
 * - login rate limiting.
 */
object AuthModule
